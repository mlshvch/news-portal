<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
<head>
  <title>NewsPortal</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>

<header>
  <nav class="navbar navbar-dark bg-dark justify-content-between" style="padding: 1%">
    <%= link_to 'NewsArticles', news_articles_url, class: 'navbar-brand' %>
    <form class="form-inline">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
    </form>
    <div>
      <% if user_signed_in? %>
        <%= link_to 'Account', request.original_url, class: "btn btn-light ", role: "button" %>
        <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: "btn btn-light ", role: "button" %>
      <% else %>
        <%= link_to 'Sign in', new_user_session_url, class: "btn btn-danger text-white ", role: "button" %>
      <% end %>
    </div>
  </nav>

  <div class="navbar navbar-light bg-light justify-content-center">
    <ul class="container" style="">
      <li>category</li>
      <li>category</li>
      <li>category</li>
      <li>category</li>
      <li>category</li>
      <li>category</li>
      <li>category</li>
      <li>category</li>
    </ul>
  </div>

</header>

<%= yield %>
</body>
</html>
