<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
<head>
  <title>NewsPortal</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <link href="app/views/feeds/rss_index.builder" rel="alternate" title="RSS feed" type="application/rss+xml">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>

<header>
  <nav class="navbar navbar-dark bg-dark justify-content-between" style="padding: 1%">
    <%= link_to 'NewsArticles', news_articles_url, class: 'navbar-brand' %>
    <form class="form-inline">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
    </form>
    <div>
      <% if user_signed_in? %>
        <%= link_to current_user.email, user_path(current_user), class: "btn btn-light ", role: "button" %>
        <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: "btn btn-light ", role: "button" %>
      <% else %>
        <%= link_to 'Sign in', new_user_session_url, class: "btn btn-danger text-white ", role: "button" %>
      <% end %>
    </div>
  </nav>

  <div class="navbar navbar-light bg-light justify-content-center">
    <ul class="container" style="">
      <li>category</li>
      <li>category</li>
      <li>category</li>
      <li>category</li>
      <li>category</li>
      <li>category</li>
      <li>category</li>
      <li>category</li>
    </ul>
  </div>

</header>
<%= yield %>

<footer class="bg-light text-lg-start text-center">
  <% if user_signed_in? %>

    <div class="text-center p-3">
      <div style="background-color: rgba(0, 0, 0, 0.2);">
        <%= link_to "RSS", feeds_rss_url, class: "link-light" %>
      </div>
    </div>
  <% end %>
</footer>
</body>
</html>
